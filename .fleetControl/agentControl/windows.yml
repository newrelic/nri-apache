namespace: newrelic
name: com.newrelic.infrastructure.nri_apache
version: 0.1.0
platform: host
operating_system: windows
protocol_version: "1.0"
variables:
  config:
    description: "nri-apache integration YAML consumed by the infra-agent"
    type: yaml
    required: true
  version:
    description: "Agent version"
    type: string
    required: true
  oci:
    repository:
      description: "OCI repository name for nri-apache"
      type: string
      required: false
      default: newrelic/nri-apache-artifacts
      variants:
        ac_config_field: "oci_repository_urls"
        values: [ "newrelic/nri-apache-artifacts" ]
deployment:
  packages:
    nri-apache:
      download:
        oci:
          repository: ${nr-var:oci.repository}
          version: ${nr-var:version}
          public_key_url: https://publickeys.newrelic.com/g/agent-control-oci/global/nri-apache/jwks.json
  shared_filesystem:
    infra-agent-ohi-configs:
      kind: dir
      entries:
        nri-apache.yaml:
          kind: file
          text: |
            ${nr-var:config}
    infra-agent-ohi-binaries:
      kind: dir
      entries:
        nri-apache.exe:
          kind: file
          copy_from_file: ${nr-sub:packages.nri-apache.dir}\\nri-apache.exe
